# Wallpapers: ~/.config/backgrounds (backgrounds package). Cycle with Super+Shift+W.
# Apply live: hyprctl hyprpaper wallpaper ",<path>"

splash = false

wallpaper {
    monitor =
    path = ~/.config/backgrounds/solid-tokyo-night.png
    fit_mode = cover
}
